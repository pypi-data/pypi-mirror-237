from setuptools import setup, find_packages

setup(
    name='beautiful_rainbow',
    version='1.0.0',
    author='ilco',
    description='A very basic package practice.',
    packages=find_packages(),
    install_requires=[],
)