[tool.poetry]
name = "grpc-kit"
version = "0.1.4"
description = ""
authors = ["miclon <jcnd@163.com>"]
readme = "README.md"
packages = [
    { include = 'grpc_kit', from = 'src' }
]

[tool.poetry.dependencies]
python = "^3.8"
grpcio = "^1.59.0"

[tool.poetry.group.dev.dependencies]
grpcio-tools = "^1.59.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
