{% if params._params.enable_gpus and experiment_overview._system_stats.num_gpus > 1 %} 

Driverless AI automatically parallelizes the training of models across the GPU’s of the machine.

{% endif %}

