[tool.poetry]
name = "temp-wake-detectors"
version = "0.1.2"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "temp_wake_detectors"}]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.plugins."woke.plugins.detectors"]
temp = "temp_wake_detectors"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
