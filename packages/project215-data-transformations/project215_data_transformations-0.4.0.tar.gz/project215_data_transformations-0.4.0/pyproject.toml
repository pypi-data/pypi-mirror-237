[tool.poetry]
name = "project215-data-transformations"
version = "0.4.0"
description = "This package includes transpose, time series windowing and cross-correlation libraries."
authors = ["marius uogle <uogelemarius@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
numpy = "1.26.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"