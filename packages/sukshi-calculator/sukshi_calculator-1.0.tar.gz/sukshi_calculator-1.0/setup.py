# setup.py

from setuptools import setup

setup(
    name='sukshi_calculator',
    version='1.0',
    packages=['sukshi_calculator'],
    install_requires=[
        # List your package dependencies here, if any
    ],
)
