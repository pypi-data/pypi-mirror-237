#!/bin/bash
#-------------------------------------------------------------------------------
# es7s/core
# (c) 2023 A. Shavykin <0.delameter@gmail.com>
#-------------------------------------------------------------------------------
__SELF="$(basename "$0" | sed -Ee 's/\..+$//')"
__USAGE="$(cat <<-EOL
Description.

Usage:
    ${__SELF} -
EOL
)"

__main() {
    :
}

# [[ $# -lt 1 ]] ||
# [[ $# -gt 1 ]] ||
[[ $* =~ (--)?help ]] && echo "$__USAGE" && exit
__main "$@"
